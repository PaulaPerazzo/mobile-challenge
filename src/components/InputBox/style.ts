import styled from 'styled-components/native';

export const Input = styled.TextInput`
  width: 80%;
  height: 50px;
  padding: 10px;

  background: #1e1f2f;
  border-radius: 6px;

  font-family: 'Raleway';
  font-style: normal;
  font-weight: 400;
  font-size: 12px;
  line-height: 18px;

  color: #ffffff;
`;
